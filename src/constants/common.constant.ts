export enum AlertLogsView {
  STOCKS,
  OPTIONS
}

export enum Sentiment {
  VeryNegative = 'very_negative',
  Negative = 'negative',
  Neutral = 'neutral',
  Positive = 'positive',
  VeryPositive = 'very_positive'
}
